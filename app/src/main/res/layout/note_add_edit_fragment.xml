<?xml version="1.0" encoding="utf-8"?>
<ScrollView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fillViewport="true"
    tools:context=".screens.addedit.ui.view.NoteAddEditFragment"
    android:background="@color/note_add_edit_fragment_background_color">

    <androidx.appcompat.widget.LinearLayoutCompat
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="@dimen/size_16">

        <EditText
            android:id="@+id/note_add_edit_edit_text_header"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@null"
            android:hint="@string/header_edit_text"
            android:textColor="@color/adaptive_text_color"
            android:textColorHint="@color/edit_text_hint_color"
            android:textSize="@dimen/text_26"
            android:inputType="textCapSentences|textMultiLine"
            tools:ignore="Autofill" />

        <EditText
            android:paddingBottom="@dimen/size_64"
            android:id="@+id/note_add_edit_desc_edit_text"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/size_16"
            android:textSize="@dimen/text_20"
            android:textColor="@color/adaptive_text_color"
            android:textColorHint="@color/edit_text_hint_color"
            android:hint="@string/start_typing_hint"
            android:background="@null"
            android:inputType="textCapSentences|textMultiLine"
            tools:ignore="Autofill" />
    </androidx.appcompat.widget.LinearLayoutCompat>
</ScrollView>